# WARNING: This file is autogenerated - changes will be overwritten if not made via https://github.com/pulumi/ci-mgmt

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
jobs:
  upgrade-provider:
    if: ${{ github.event.pull_request.title }} =~ 'Upgrade terraform-provider-'
    name: upgrade
    runs-on: ubuntu-latest
    steps:
    - uses: pulumi/pulumi-upgrade-provider-action@v0.0.2
      with:
        provider-repo: "pulumi-spotinst"
        gh-token: ${{ secrets.PULUMI_BOT_TOKEN }}
        branch: "master"
        slack-webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
        slack-channel: "provider-upgrade-status"
        git-username: "Pulumi bot"
        git-email: "bot@pulumi.com"
name: Upgrade provider
on:
  pull_request:
    types:
    - opened
  push: 
